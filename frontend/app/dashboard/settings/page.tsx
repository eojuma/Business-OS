"use client";
import { useEffect,useState } from "react";import{api}from"@/lib/api";import{PageHeader}from"@/components/app-shell";import{UserPlus,Users}from"lucide-react";
type User={id:string;name:string;email:string;role:string;active:boolean};export default function SettingsPage(){const[users,setUsers]=useState<User[]>([]),[name,setName]=useState(""),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[role,setRole]=useState("cashier"),[error,setError]=useState("");async function load(){try{const r=await api.get("/users");setUsers(r.data.data||[])}catch(e:any){setError(e.response?.data?.error||"Only owners can manage users")}}useEffect(()=>{load()},[]);async function add(e:React.FormEvent){e.preventDefault();try{await api.post("/users",{name,email,password,role});setName("");setEmail("");setPassword("");await load()}catch(e:any){setError(e.response?.data?.error||"Failed to add user")}}return <><PageHeader eyebrow="Workspace" title="Settings" description="Manage the people who can access this business workspace."/><section className="grid gap-6 xl:grid-cols-[.8fr_1.2fr]"><form onSubmit={add} className="panel space-y-4 p-5"><div><p className="eyebrow">Team access</p><h2 className="mt-1 text-lg font-bold">Add a team member</h2></div>{error&&<p className="rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<input className="field" placeholder="Full name" value={name} onChange={e=>setName(e.target.value)} required/><input className="field" type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><input className="field" type="password" minLength={8} placeholder="Temporary password" value={password} onChange={e=>setPassword(e.target.value)} required/><select className="field" value={role} onChange={e=>setRole(e.target.value)}><option value="cashier">Cashier</option><option value="manager">Manager</option><option value="owner">Owner</option></select><button className="btn-primary"><UserPlus size={16}/> Add member</button></form><div className="panel overflow-hidden"><div className="border-b border-[#eef1ed] p-5"><p className="eyebrow">Access list</p><h2 className="mt-1 text-lg font-bold">Workspace members</h2></div>{users.length===0?<div className="p-8 text-center text-sm text-[#718078]"><Users className="mx-auto mb-2"/>No members found.</div>:<div>{users.map(u=><div key={u.id} className="flex items-center gap-3 border-t border-[#eef1ed] px-5 py-4"><span className="flex h-9 w-9 items-center justify-center rounded-full bg-[#e7f4ec] text-sm font-bold text-[#16794c]">{u.name.slice(0,1).toUpperCase()}</span><div className="min-w-0 flex-1"><p className="text-sm font-semibold">{u.name}</p><p className="text-xs text-[#718078]">{u.email}</p></div><span className="rounded-full bg-[#f2f5f1] px-2.5 py-1 text-xs font-bold capitalize text-[#647168]">{u.role}</span></div>)}</div>}</div></section></>}
