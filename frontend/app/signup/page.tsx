"use client";
import Link from "next/link";import{useState}from"react";import{useRouter}from"next/navigation";import{ArrowRight,Check,Store}from"lucide-react";import{api}from"@/lib/api";
export default function SignupPage(){const router=useRouter();const[businessName,setBusinessName]=useState("");const[phone,setPhone]=useState("");const[ownerName,setOwnerName]=useState("");const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setError("");setLoading(true);try{const b=await api.post("/business",{name:businessName,phone});const r=await api.post("/auth/register",{business_id:b.data.data.id,name:ownerName,email,password});localStorage.setItem("token",r.data.data.token);router.push("/dashboard")}catch(e:any){setError(e.response?.data?.error||"Unable to create workspace")}finally{setLoading(false)}}return <main className="min-h-screen bg-[#f7f9f6]"><nav className="mx-auto flex max-w-7xl items-center justify-between px-5 py-5 lg:px-8"><Link href="/" className="text-xl font-bold">Business <span className="text-[#16794c]">OS</span></Link><p className="text-sm text-[#718078]">Already have an account? <Link href="/login" className="font-semibold text-[#16794c]">Sign in</Link></p></nav><div className="mx-auto grid max-w-5xl gap-12 px-5 pb-16 pt-8 lg:grid-cols-[.8fr_1.2fr] lg:items-center lg:px-8"><div><span className="mb-6 inline-flex rounded-xl bg-[#e7f4ec] p-3 text-[#16794c]"><Store size={23}/></span><h1 className="text-4xl font-bold tracking-tight">A better operating rhythm starts here.</h1><p className="mt-4 leading-7 text-[#718078]">Set up your store workspace and bring the everyday work into one place.</p><div className="mt-8 space-y-3 text-sm text-[#526057]"><p className="flex gap-2"><Check size={17} className="text-[#16794c]"/> Products and stock in one view</p><p className="flex gap-2"><Check size={17} className="text-[#16794c]"/> Sales, credit and supplier balances</p><p className="flex gap-2"><Check size={17} className="text-[#16794c]"/> Business insights when you need them</p></div></div><form onSubmit={submit} className="panel space-y-4 p-6 sm:p-8"><div><p className="eyebrow mb-2">Get started</p><h2 className="text-2xl font-bold">Create your workspace</h2></div>{error&&<p className="rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold sm:col-span-2">Store name<input className="field mt-1.5" value={businessName} onChange={e=>setBusinessName(e.target.value)} required/></label><label className="text-sm font-semibold">Your name<input className="field mt-1.5" value={ownerName} onChange={e=>setOwnerName(e.target.value)} required/></label><label className="text-sm font-semibold">Phone<input className="field mt-1.5" value={phone} onChange={e=>setPhone(e.target.value)}/></label><label className="text-sm font-semibold sm:col-span-2">Email<input className="field mt-1.5" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label className="text-sm font-semibold sm:col-span-2">Password<input className="field mt-1.5" type="password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)} required/><span className="mt-1 block text-xs font-normal text-[#8a958d]">Use at least 8 characters.</span></label></div><button className="btn-primary w-full py-3" disabled={loading}>{loading?"Creating workspace":"Create workspace"}<ArrowRight size={16}/></button></form></div></main>}
